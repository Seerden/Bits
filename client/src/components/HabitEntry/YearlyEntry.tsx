import { EntryProps } from "types/HabitEntry";

const YearlyEntry = (props: EntryProps) => {
    const base = "YearlyEntry";
    
    return (
        <div className={`${base}`}>
            
        </div>
    )
}

export default YearlyEntry