import React from "react";

const WeeklyEntry = ({ completionEntries }) => {
    const base = "WeeklyEntry";
    
    return (
        <div className={`${base}`}>
            
        </div>
    )
};

export default WeeklyEntry